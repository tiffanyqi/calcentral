'use strict';

var angular = require('angular');

/**
 * Temporary final exam schedule factory
 */
angular.module('calcentral.factories').factory('finalExamScheduleFactory', function(apiService, $route) {
  // var instructions_url = '/api/final_exam_schedule';
  var enrollments_url = '/dummy/json/class_enrollments.json';
  
});
